main = f z 42 4;

z : Nat;
z = 3;

f : Nat -> Nat -> Nat -> Nat;
f x j p = j + k 3 - p; {
  k : Nat -> Nat;
  k l = l + x; {
    p : Nat; p = 15;
    q : Bool; q = True; 
    r : T<3>; r = [1. 2. 3. ];
  }
}
